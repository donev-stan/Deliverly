import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.deliverly',
  appName: 'deliverly',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
